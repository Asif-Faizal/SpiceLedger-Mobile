import 'package:flutter/material.dart';

class SetGradePricePage extends StatelessWidget {
  const SetGradePricePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Set Grade Price')),
      body: const Center(child: Text('Set Grade Price')),
    );
  }
}
